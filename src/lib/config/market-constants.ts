/**
 * Constants for market and event behavior
 */

/**
 * Percentage threshold at which a market is considered effectively resolved
 * When an outcome reaches this probability (99%), it's shown as resolved
 */
export const RESOLVED_THRESHOLD = 99;

/**
 * Number of days before end date when an event is considered "closing soon"
 */
export const CLOSING_SOON_DAYS = 7;
